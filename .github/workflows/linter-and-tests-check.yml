name: linter-and-tests
on: [push]
jobs:
  Make-lint-and-tests:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
        - uses: actions/checkout@v2
        - name: Linter & Tests
          run: |
            make install
            make lint
            make pytest